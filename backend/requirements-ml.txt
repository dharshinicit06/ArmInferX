# ML runtime dependencies for the ArmInferX baseline model (Qwen2.5-3B-Instruct).
#
# Install AFTER `pip install -r requirements.txt` (see backend/README.md):
#
#     pip install -r requirements-ml.txt
#
# torch is pulled from PyTorch's CPU wheel index so the install stays small
# and needs no CUDA. On Linux/GPU machines, drop the --extra-index-url line
# and install torch from PyPI instead.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.13.0+cpu
transformers==5.14.1
accelerate==1.14.0
huggingface_hub==1.26.0
