"""ArmInferX FastAPI application package.

- ``api.routes.inference``: domain logic (model loading + text generation).
- ``api.utils``: shared cross-cutting helpers (exceptions, logging, HTTP errors).
- ``main.py`` (sibling of this package): composition root / entry point.
"""
